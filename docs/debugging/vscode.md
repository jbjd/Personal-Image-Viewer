# Running With Debugging Enabled

Use the launch.json and settings.json in this folder as an example. Copy them to the .vscode folder in root. You should then be able to run it with debugging by going to "Run and Debug" > "Start Image Viewer".
