# Running With Debugging Enabled

I will assume you are using vscode.

Use the launch.json in this folder as an example, copy it to the .vscode folder in root. You should then be able to run it with debugging by going to "Run and Debug" > "Start Image Viewer".
